# Snippets

```js
  javascript:void(
    function(){
      const els = document.querySelectorAll('img');
      els.forEach(item => {
        item.style = "visibility: hidden;"
      })
    }
  )();
```
